<% @portfolio.posts.each do |p| %>
  <h3>
    <%= p.title %>
  </h3>
  <div>
    <p>
      <%= p.subtitle %>
    </p>
    <p>
      <%= time_ago_in_words(p.created_at) %> ago
    </p>
    <p>
      <%= p.body%>
    </p>
  </div>
<% end %>